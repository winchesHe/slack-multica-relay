{
  "name": "slack-multica-relay",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.34.5",
  "engines": {
    "node": ">=22.12.0"
  },
  "scripts": {
    "test": "vitest run && python3 -m unittest discover -s tests -p '*_test.py'",
    "lint": "tsc --noEmit -p tsconfig.json",
    "dev:cf": "wrangler dev",
    "build:cf": "wrangler deploy --dry-run",
    "deploy:cf": "wrangler deploy"
  },
  "devDependencies": {
    "@types/node": "^24.10.1",
    "typescript": "^6.0.3",
    "vitest": "^4.1.8",
    "wrangler": "4.129.1"
  },
  "dependencies": {
    "@upstash/qstash": "^2.11.3"
  }
}
