# Mention 取消任务交付记录

## 当前实现

配置、事件接入、Multica 取消、reaction 清理及失败恢复已在同一 PR 分支完成。入口为原任务 thread 中的目标 mention 加取消关键词；不增加 Slack 事件订阅。此前基于 Autopilot 的设计已按当前主分支的直接建卡与 QStash 架构改写。

| 情况 | 行为 |
| --- | --- |
| 发送者不在 SLACK_TARGET_USER_IDS | 忽略，不进入任务派发 |
| 没有关联任务 | 不建卡，记录时间边界阻止早于取消的排队请求晚到后启动 |
| 建卡结果未知 | 保存取消意图，按来源 marker 回读，确认前进入队列重试 |
| 已有 issue 尚无运行 | 等待重试，不把空结果当作已完成 |
| 排队、执行、等待目录或 deferred | 固定运行 ID 集合，逐一取消并回读 |
| 已完成或失败且无活动运行 | 不取消、不清理 |
| 取消时恰好自然完成 | 无实际 cancelled 运行时返回 no_active_run 并保留表情 |
| 取消请求结果未知 | 下次先回读状态，仅重试仍活动的原运行 |
| 取消成功、清理失败 | 保留 cleaning 阶段及剩余消息，重试只补清理 |
| 多次取消或重复投递 | 共用线程锁和取消进度，不重新选择后来创建的运行 |
| 取消期间收到任务消息 | 忽略，不建卡或追加评论 |
| 取消后发送新任务 | 复用原卡；旧事件受时间边界限制 |
| 外部创建了快照以外的新运行 | 不取消新运行，停止清理并暴露 cancellation_new_run |

## 范围与验证

- 关键词默认 cancel、取消；非空自定义列表替换默认值，完整匹配且英文忽略大小写。
- 入队时固定指令类型，消费时重新检查发送者权限及原有准入策略。
- 本次直接建卡架构无 Autopilot run ID；取消使用从 issue 运行列表读取的 Agent run ID。
- 正常事件的启动 reaction 与取消共用线程锁，避免 Relay 自身迟到回写。
- 本地测试覆盖上述主要路径，真实 Slack、Multica、QStash 联调与部署按搭建手册执行。
- Multica 的 cancelled 终态是本实现的确认依据；接口未提供独立的 daemon 停止确认，已完成的外部操作不会撤销。
